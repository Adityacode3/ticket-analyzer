# frontend/Dockerfile.dev
# Development-only Dockerfile that runs the Vite dev server
# with hot module replacement instead of a static nginx build.

FROM node:20-alpine

WORKDIR /app

COPY package.json ./

RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]